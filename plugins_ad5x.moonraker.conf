[plugins_ad5x]
# Safety gate: repository integration may observe/adopt the existing Z-Mod
# offset lifecycle, but Plugins AD5X must not write a composed Z offset until
# controlled hardware acceptance explicitly enables this option.
z_offset_writes_enabled: false
